{
  "name": "@mimex/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mimex": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests for cli yet'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mimex/core": "workspace:*",
    "commander": "^13.1.0"
  }
}
